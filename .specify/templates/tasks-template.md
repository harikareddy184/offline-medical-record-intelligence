# Task List

## Feature

- Feature ID:
- Related spec:
- Owner:

## Implementation Tasks

- [ ] Confirm the workflow stays offline and CPU-first.
- [ ] Update parser, UI, documentation, or tests in the smallest useful scope.
- [ ] Use synthetic medical examples only.
- [ ] Document user-visible behavior and JSON contract changes.

## Validation Tasks

- [ ] Run `python -m compileall app.py backend scripts tests`.
- [ ] Run `ruff check .`.
- [ ] Run `mypy backend scripts --ignore-missing-imports`.
- [ ] Run `pytest -q`.
- [ ] Run `bandit -r backend scripts -ll`.

## Release Notes

- User impact:
- Privacy impact:
- Follow-up work:
